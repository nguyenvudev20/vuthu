streamlit>=1.36
pillow>=10.0.0
numpy>=1.24
moviepy>=1.0.3
# Optional: try to use TF for ML effect selection. If deploy size/time is a concern, you can remove it.
tensorflow-cpu>=2.15; platform_machine!="aarch64"